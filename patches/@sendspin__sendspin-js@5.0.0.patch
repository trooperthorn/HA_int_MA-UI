diff --git a/dist/audio/decoder.d.ts b/dist/audio/decoder.d.ts
index b304628dff7028d93377eae51648516d6635ab75..e85a087f5f9d963fcbb4b75def98641d59f66bac 100644
--- a/dist/audio/decoder.d.ts
+++ b/dist/audio/decoder.d.ts
@@ -12,6 +12,7 @@ export declare class SendspinDecoder {
     private webCodecsDecoderReady;
     private webCodecsFormat;
     private useNativeOpus;
+    private useNativeFlac;
     private nativeDecoderQueue;
     private opusDecoder;
     private opusDecoderModule;
@@ -29,6 +30,9 @@ export declare class SendspinDecoder {
     private decodePCM;
     private getFlacDecodingContext;
     private decodeFLAC;
+    private nativeDecoderWanted;
+    private disableNativeDecoder;
+    private webCodecsConfig;
     private initWebCodecsDecoder;
     private createWebCodecsDecoder;
     private handleAudioData;
diff --git a/dist/audio/decoder.js b/dist/audio/decoder.js
index 8a5d7a449beadbe2dfb1251b3e72f7fe1f35c110..7e917a2930e7b134f719fc73191e096bda34363c 100644
--- a/dist/audio/decoder.js
+++ b/dist/audio/decoder.js
@@ -11,6 +11,8 @@ export class SendspinDecoder {
         this.webCodecsDecoderReady = null;
         this.webCodecsFormat = null;
         this.useNativeOpus = true;
+        // Native FLAC decoder (WebCodecs API, description = codec_header)
+        this.useNativeFlac = true;
         this.nativeDecoderQueue = [];
         // Fallback Opus decoder (opus-encdec library)
         this.opusDecoder = null;
@@ -37,10 +39,10 @@ export class SendspinDecoder {
             const serverTimeUs = Number(timestampView.getBigInt64(0, false));
             // Rest is audio data
             const audioData = data.slice(9);
-            // For Opus: use native decoder (non-blocking async path)
-            if (format.codec === "opus" && this.useNativeOpus) {
+            // For Opus and FLAC: use native decoder (non-blocking async path)
+            if (this.nativeDecoderWanted(format)) {
                 await this.initWebCodecsDecoder(format);
-                if (this.useNativeOpus && this.webCodecsDecoder) {
+                if (this.nativeDecoderWanted(format) && this.webCodecsDecoder) {
                     if (this.queueToNativeOpusDecoder(audioData, serverTimeUs, generation)) {
                         return; // Async path - callback handles output
                     }
@@ -158,13 +160,42 @@ export class SendspinDecoder {
         }
     }
     // ========================================
-    // Opus - Native WebCodecs Decoder
+    // Opus and FLAC - Native WebCodecs Decoder
     // ========================================
+    /** Whether this format goes through WebCodecs (until it proves unable to). */
+    nativeDecoderWanted(format) {
+        if (format.codec === "opus")
+            return this.useNativeOpus;
+        // FLAC needs the stream header as the decoder description
+        if (format.codec === "flac")
+            return this.useNativeFlac && !!format.codec_header;
+        return false;
+    }
+    disableNativeDecoder(format) {
+        if (format.codec === "opus")
+            this.useNativeOpus = false;
+        else if (format.codec === "flac")
+            this.useNativeFlac = false;
+    }
+    /** The AudioDecoderConfig for a format; FLAC carries fLaC + STREAMINFO. */
+    webCodecsConfig(format) {
+        const config = {
+            codec: format.codec,
+            sampleRate: format.sample_rate,
+            numberOfChannels: format.channels,
+        };
+        if (format.codec === "flac" && format.codec_header) {
+            config.description = Uint8Array.from(atob(format.codec_header), (c) => c.charCodeAt(0));
+        }
+        return config;
+    }
     async initWebCodecsDecoder(format) {
         const tryConfigureExistingDecoder = () => {
             if (!this.webCodecsDecoder)
                 return false;
             const matchesFormat = !!this.webCodecsFormat &&
+                this.webCodecsFormat.codec === format.codec &&
+                this.webCodecsFormat.codec_header === format.codec_header &&
                 this.webCodecsFormat.sample_rate === format.sample_rate &&
                 this.webCodecsFormat.channels === format.channels;
             if (this.webCodecsDecoder.state === "configured" && matchesFormat) {
@@ -174,11 +205,7 @@ export class SendspinDecoder {
                 return false;
             }
             try {
-                this.webCodecsDecoder.configure({
-                    codec: "opus",
-                    sampleRate: format.sample_rate,
-                    numberOfChannels: format.channels,
-                });
+                this.webCodecsDecoder.configure(this.webCodecsConfig(format));
                 this.webCodecsFormat = format;
                 return true;
             }
@@ -213,37 +240,31 @@ export class SendspinDecoder {
     }
     async createWebCodecsDecoder(format) {
         if (typeof AudioDecoder === "undefined") {
-            this.useNativeOpus = false;
+            this.disableNativeDecoder(format);
             return;
         }
+        const tag = format.codec === "flac" ? "[NativeFlac]" : "[NativeOpus]";
         try {
-            const support = await AudioDecoder.isConfigSupported({
-                codec: "opus",
-                sampleRate: format.sample_rate,
-                numberOfChannels: format.channels,
-            });
+            const config = this.webCodecsConfig(format);
+            const support = await AudioDecoder.isConfigSupported(config);
             if (!support.supported) {
-                console.log("[NativeOpus] WebCodecs Opus not supported, will use fallback");
-                this.useNativeOpus = false;
+                console.log(`${tag} WebCodecs ${format.codec} not supported, will use fallback`);
+                this.disableNativeDecoder(format);
                 return;
             }
             this.webCodecsDecoder = new AudioDecoder({
                 output: (audioData) => this.handleAudioData(audioData),
                 error: (error) => {
-                    console.error("[NativeOpus] WebCodecs decoder error:", error);
+                    console.error(`${tag} WebCodecs decoder error:`, error);
                 },
             });
-            this.webCodecsDecoder.configure({
-                codec: "opus",
-                sampleRate: format.sample_rate,
-                numberOfChannels: format.channels,
-            });
+            this.webCodecsDecoder.configure(config);
             this.webCodecsFormat = format;
-            console.log(`[NativeOpus] Using WebCodecs AudioDecoder: ${format.sample_rate}Hz, ${format.channels}ch`);
+            console.log(`${tag} Using WebCodecs AudioDecoder: ${format.sample_rate}Hz, ${format.channels}ch`);
         }
         catch (error) {
-            console.warn("[NativeOpus] WebCodecs init failed, will use fallback:", error);
-            this.useNativeOpus = false;
+            console.warn(`${tag} WebCodecs init failed, will use fallback:`, error);
+            this.disableNativeDecoder(format);
         }
     }
     // Handle decoded audio data from native Opus decoder
diff --git a/dist/audio/scheduler.d.ts b/dist/audio/scheduler.d.ts
index 872beb035e861ab6f81ba0a98b711c5336d1a97a..8e6ede84f47d1cd020d959803cfb1da75f357b36 100644
--- a/dist/audio/scheduler.d.ts
+++ b/dist/audio/scheduler.d.ts
@@ -21,6 +21,8 @@ export interface AudioSchedulerOptions {
     silentAudioSrc?: string;
     syncDelayMs?: number;
     useHardwareVolume?: boolean;
+    /** AudioContext latencyHint; the browser default ("interactive") when unset. */
+    latencyHint?: AudioContextLatencyCategory | number;
     correctionMode?: CorrectionMode;
     storage?: SendspinStorage | null;
     useOutputLatencyCompensation?: boolean;
@@ -63,6 +65,7 @@ export declare class AudioScheduler {
     private silentAudioSrc?;
     private syncDelayMs;
     private useHardwareVolume;
+    private latencyHint;
     constructor(options: AudioSchedulerOptions);
     get correctionMode(): CorrectionMode;
     setCorrectionMode(mode: CorrectionMode): void;
diff --git a/dist/audio/scheduler.js b/dist/audio/scheduler.js
index b06314d3dcf68511af39f63168709d53d7752ed6..9613529b28b3d0c0fe034d3a48a9dc26e79e7b03 100644
--- a/dist/audio/scheduler.js
+++ b/dist/audio/scheduler.js
@@ -101,6 +101,7 @@ export class AudioScheduler {
         this.silentAudioSrc = options.silentAudioSrc;
         this.syncDelayMs = clampSyncDelayMs(options.syncDelayMs ?? 0);
         this.useHardwareVolume = options.useHardwareVolume ?? false;
+        this.latencyHint = options.latencyHint;
         this._correctionMode = options.correctionMode ?? "sync";
         this.useOutputLatencyCompensation =
             options.useOutputLatencyCompensation ?? true;
@@ -428,7 +429,12 @@ export class AudioScheduler {
             navigator.audioSession.type = "playback";
         }
         const streamSampleRate = this.stateManager.currentStreamFormat?.sample_rate || 48000;
-        this.audioContext = new AudioContext({ sampleRate: streamSampleRate });
+        // A buffered stream can take the larger, power-friendly output buffer
+        // that "playback" asks for; the output-latency tracker measures and
+        // compensates whatever the platform then adds.
+        this.audioContext = new AudioContext(this.latencyHint === undefined
+            ? { sampleRate: streamSampleRate }
+            : { sampleRate: streamSampleRate, latencyHint: this.latencyHint });
         this.gainNode = this.audioContext.createGain();
         const audioElement = this.audioElement;
         if (this.outputMode === "direct") {
diff --git a/dist/core/core.d.ts b/dist/core/core.d.ts
index 9a9ed048d6da3eb3e8bca86d1d656b91bebf9bfe..789d31b4268e98ebc0aec8e5eb01fd1b39f2d0a5 100644
--- a/dist/core/core.d.ts
+++ b/dist/core/core.d.ts
@@ -66,6 +66,8 @@ export declare class SendspinCore implements StreamHandler {
     setRequiredLeadTimeMs(leadTimeMs: number): void;
     setMinBufferMs(minBufferMs: number): void;
     sendCommand<T extends ControllerCommand>(command: T, params: ControllerCommands[T]): void;
+    setAvailable(available: boolean): void;
+    leaveGroup(): void;
     get isPlaying(): boolean;
     get volume(): number;
     get muted(): boolean;
diff --git a/dist/core/core.js b/dist/core/core.js
index 45f85ba2227cba75cd308a01b240c3962d83008b..6e49bf13ca3c23eca3501fa3263972eb3cf162d2 100644
--- a/dist/core/core.js
+++ b/dist/core/core.js
@@ -90,6 +90,7 @@ export class SendspinCore {
         this.protocolHandler = new ProtocolHandler(this.transport, helloContext, this, // this class implements StreamHandler
         this.stateManager, this.timeFilter, {
             clientName,
+            supportedRoles: config.supportedRoles,
             productName: config.productName,
             codecs: config.codecs,
             bufferCapacity: config.bufferCapacity,
@@ -301,12 +302,29 @@ export class SendspinCore {
     // ========================================
     sendCommand(command, params) {
         const supportedCommands = this.stateManager.serverState.controller?.supported_commands;
-        if (supportedCommands && !supportedCommands.includes(command)) {
+        if (!Array.isArray(supportedCommands) || !supportedCommands.includes(command)) {
             throw new Error(`Command '${command}' is not supported by the server. ` +
-                `Supported commands: ${supportedCommands.join(", ")}`);
+                `Supported commands: ${supportedCommands?.join(", ") ?? "none reported"}`);
+        }
+        if (command === "seek") {
+            const position = params?.position_ms;
+            const maximum = this.stateManager.serverState.controller?.seek_max_ms;
+            if (!Number.isSafeInteger(position) || position < 0 ||
+                !Number.isSafeInteger(maximum) || position > maximum) {
+                throw new RangeError("seek requires an integer position_ms within the server's seekable range");
+            }
+        }
+        if (command === "seek_relative" && !Number.isSafeInteger(params?.offset_ms)) {
+            throw new RangeError("seek_relative requires an integer offset_ms");
         }
         this.protocolHandler.sendCommand(command, params);
     }
+    setAvailable(available) {
+        this.protocolHandler.setAvailable(available);
+    }
+    leaveGroup() {
+        this.protocolHandler.leaveGroup();
+    }
     // ========================================
     // State getters
     // ========================================
diff --git a/dist/core/protocol-handler.d.ts b/dist/core/protocol-handler.d.ts
index a8fddccfd7774474846d68413baf8b89e3670e09..be46b87db54bcc00d2cd43347fcfb647ba0c7ad8 100644
--- a/dist/core/protocol-handler.d.ts
+++ b/dist/core/protocol-handler.d.ts
@@ -12,6 +12,7 @@ export interface HelloContext {
     unpairedAccess: boolean;
 }
 export interface ProtocolHandlerConfig {
+    supportedRoles?: Array<"player@v1" | "controller@v1" | "metadata@v1">;
     clientName?: string;
     productName?: string;
     codecs?: Codec[];
@@ -33,6 +34,8 @@ export declare class ProtocolHandler {
     private stateManager;
     private timeFilter;
     private clientName;
+    private supportedRoles;
+    private available;
     private productName?;
     private codecs;
     private bufferCapacity;
@@ -68,6 +71,8 @@ export declare class ProtocolHandler {
     setRequiredLeadTimeMs(leadTimeMs: number): void;
     setMinBufferMs(minBufferMs: number): void;
     sendStateUpdate(skipHardwareRead?: boolean): void;
+    setAvailable(available: boolean): void;
+    leaveGroup(): void;
     sendGoodbye(reason: GoodbyeReason): void;
     sendCommand<T extends ControllerCommand>(command: T, params: ControllerCommands[T]): void;
 }
diff --git a/dist/core/protocol-handler.js b/dist/core/protocol-handler.js
index a77a065b09674c70e8fa581eaba19a29aeb7fef6..c94eb0a2769222e8f6ca26865ceab890439bb42d 100644
--- a/dist/core/protocol-handler.js
+++ b/dist/core/protocol-handler.js
@@ -25,6 +25,14 @@ export class ProtocolHandler {
         // full again.
         this.lastSentPlayer = null;
         this.clientName = config.clientName ?? "Sendspin Player";
+        const roles = config.supportedRoles ?? ["player@v1", "controller@v1", "metadata@v1"];
+        if (!Array.isArray(roles) || roles.length === 0 ||
+            roles.some((role) => !["player@v1", "controller@v1", "metadata@v1"].includes(role)) ||
+            new Set(roles).size !== roles.length) {
+            throw new RangeError("supportedRoles must contain unique implemented Sendspin roles");
+        }
+        this.supportedRoles = roles;
+        this.available = true;
         this.productName = config.productName;
         this.codecs = config.codecs ?? ["opus", "flac", "pcm"];
         // Left undefined so the capacity is derived from the formats actually
@@ -215,12 +223,13 @@ export class ProtocolHandler {
     }
     // client_id and version live in client/init, not the hello.
     sendClientHello() {
-        const supportedFormats = getSupportedFormats(this.codecs);
+        const playerRole = this.supportedRoles.includes("player@v1");
+        const supportedFormats = playerRole ? getSupportedFormats(this.codecs) : [];
         const hello = {
             type: "client/hello",
             payload: {
                 name: this.clientName,
-                supported_roles: ["player@v1", "controller@v1", "metadata@v1"],
+                supported_roles: this.supportedRoles,
                 trust_level: this.helloContext.trustLevel(),
                 supported_pair_methods: this.helloContext.pairMethods(),
                 unpaired_access: { enabled: this.helloContext.unpairedAccess },
@@ -230,11 +239,11 @@ export class ProtocolHandler {
                     software_version: (typeof navigator !== "undefined" && navigator.userAgent) ||
                         "Unknown",
                 },
-                "player@v1_support": {
+                ...(playerRole ? { "player@v1_support": {
                     supported_formats: supportedFormats,
                     buffer_capacity: this.bufferCapacity ?? getDefaultBufferCapacity(supportedFormats),
                     supported_commands: ["volume", "mute"],
-                },
+                } } : {}),
             },
         };
         // Reset so the first client/state after connect is a full snapshot.
@@ -268,9 +277,10 @@ export class ProtocolHandler {
         const syncDelayMs = this.streamHandler.getSyncDelayMs();
         const staticDelayMs = clampSyncDelayMs(syncDelayMs);
         const payload = {
-            available: true,
+            available: this.available,
         };
-        if (this.activeRoles === null || this.activeRoles.has("player@v1")) {
+        if (this.supportedRoles.includes("player@v1") &&
+            (this.activeRoles === null || this.activeRoles.has("player@v1"))) {
             const current = {
                 volume,
                 muted,
@@ -309,6 +319,20 @@ export class ProtocolHandler {
         };
         this.sender.sendControl(message);
     }
+    setAvailable(available) {
+        if (typeof available !== "boolean")
+            throw new TypeError("available must be a boolean");
+        if (this.available === available)
+            return;
+        this.available = available;
+        if (this.activated && !this.pairingSuspended)
+            this.sendStateUpdate();
+    }
+    leaveGroup() {
+        if (!this.activated || this.pairingSuspended)
+            return;
+        this.sender.sendControl({ type: "client/leave", payload: {} });
+    }
     // Send goodbye message before disconnecting
     sendGoodbye(reason) {
         this.sender.sendControl({
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 862743776d68a8975f71c12a7d01e3fce8cb45b5..433e326497bcf01a39333d186d1ba62d99a01aa6 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -48,6 +48,10 @@ export declare class SendspinPlayer {
      * Send a controller command to the server.
      */
     sendCommand<T extends ControllerCommand>(command: T, params: ControllerCommands[T]): void;
+    /** Mark this client busy with external activity or ready for Sendspin again. */
+    setAvailable(available: boolean): void;
+    /** Leave the current playing group without disconnecting. */
+    leaveGroup(): void;
     get isPlaying(): boolean;
     get volume(): number;
     get muted(): boolean;
diff --git a/dist/index.js b/dist/index.js
index 1e4ff4cc5dab7c11d266b8af1344b8dca3a1841c..92c4fff7b5aeecb6179f41b4541bf2d9f7784b01 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -52,6 +52,7 @@ export class SendspinPlayer {
         this.core = new SendspinCore({
             baseUrl: config.baseUrl,
             clientName: config.clientName,
+            supportedRoles: config.supportedRoles,
             productName: config.productName,
             webSocket: config.webSocket,
             codecs: config.codecs,
@@ -92,6 +93,7 @@ export class SendspinPlayer {
             syncDelayMs: syncDelay,
             useHardwareVolume: config.useHardwareVolume ?? false,
             correctionMode: config.correctionMode ?? "sync",
+            latencyHint: config.latencyHint,
             storage,
             useOutputLatencyCompensation: config.useOutputLatencyCompensation ?? true,
             correctionThresholds: config.correctionThresholds,
@@ -236,6 +238,12 @@ export class SendspinPlayer {
     sendCommand(command, params) {
         this.core.sendCommand(command, params);
     }
+    setAvailable(available) {
+        this.core.setAvailable(available);
+    }
+    leaveGroup() {
+        this.core.leaveGroup();
+    }
     // Getters for reactive state
     get isPlaying() {
         return this.core.isPlaying;
diff --git a/dist/types.d.ts b/dist/types.d.ts
index a8091286bdb0e5a1e8b19d31eab3ce888f18136b..5863e1eb37ae0a8a1fc28021d4dc99aa422197b2 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -6,6 +6,7 @@ export declare enum MessageType {
     CLIENT_STATE = "client/state",
     SERVER_STATE = "server/state",
     CLIENT_COMMAND = "client/command",
+    CLIENT_LEAVE = "client/leave",
     CLIENT_GOODBYE = "client/goodbye",
     SERVER_COMMAND = "server/command",
     STREAM_START = "stream/start",
@@ -63,6 +64,12 @@ export interface ControllerCommands {
     shuffle: void;
     unshuffle: void;
     switch: void;
+    seek: {
+        position_ms: number;
+    };
+    seek_relative: {
+        offset_ms: number;
+    };
 }
 export type ControllerCommand = keyof ControllerCommands;
 export interface ClientHello {
@@ -118,6 +125,10 @@ export interface ClientGoodbye {
         reason: GoodbyeReason;
     };
 }
+export interface ClientLeave {
+    type: MessageType.CLIENT_LEAVE;
+    payload: Record<string, never>;
+}
 export interface ClientCommand {
     type: MessageType.CLIENT_COMMAND;
     payload: {
@@ -125,6 +136,8 @@ export interface ClientCommand {
             command: ControllerCommand;
             volume?: number;
             mute?: boolean;
+            position_ms?: number;
+            offset_ms?: number;
         };
     };
 }
@@ -160,6 +173,7 @@ export interface ServerStateController {
     supported_commands?: string[];
     volume?: number;
     muted?: boolean;
+    seek_max_ms?: number;
 }
 export interface ServerStatePlayer {
 }
@@ -339,7 +353,7 @@ export interface ServerUnpair {
     payload: Record<string, unknown>;
 }
 export type ServerMessage = ServerHello | ServerInit | NoiseHandshake | ServerActivate | ServerPairInit | ServerPairAuth | ServerPairConfirm | ServerPairFinalize | ServerUnpair | PairAbort | ServerTime | ServerState | StreamStart | StreamClear | StreamEnd | ServerCommand | GroupUpdate;
-export type ClientMessage = ClientHello | ClientTime | ClientState | ClientCommand | ClientGoodbye;
+export type ClientMessage = ClientHello | ClientTime | ClientState | ClientCommand | ClientGoodbye | ClientLeave;
 export type StreamFormat = {
     codec: string;
     sample_rate: number;
@@ -420,6 +434,13 @@ export interface SendspinPlayerConfig extends SendspinCoreConfig {
      * Default: true
      */
     useOutputLatencyCompensation?: boolean;
+    /**
+     * latencyHint for the AudioContext. The browser default ("interactive")
+     * asks for the smallest output buffer; "playback" lets a buffered stream
+     * use a larger, power-friendly one. The extra output latency is measured
+     * and compensated like any other.
+     */
+    latencyHint?: AudioContextLatencyCategory | number;
 }
 /**
  * A decoded audio chunk with raw PCM samples.
@@ -483,6 +504,8 @@ export interface ReconnectConfig {
  * Configuration for SendspinCore (protocol + decoding, no playback).
  */
 export interface SendspinCoreConfig {
+    /** Roles this client implements. A controller-only client need not advertise a player. */
+    supportedRoles?: Array<"player@v1" | "controller@v1" | "metadata@v1">;
     /**
      * Base URL of the Sendspin server (e.g., "http://192.168.1.100:8095").
      * Required unless webSocket is provided.
diff --git a/dist/types.js b/dist/types.js
index 9802a215e527494d377f582926af09e2e2582c10..e2e5f874ca8c77b3e2c34ff45af5c53558182aba 100644
--- a/dist/types.js
+++ b/dist/types.js
@@ -8,6 +8,7 @@ export var MessageType;
     MessageType["CLIENT_STATE"] = "client/state";
     MessageType["SERVER_STATE"] = "server/state";
     MessageType["CLIENT_COMMAND"] = "client/command";
+    MessageType["CLIENT_LEAVE"] = "client/leave";
     MessageType["CLIENT_GOODBYE"] = "client/goodbye";
     MessageType["SERVER_COMMAND"] = "server/command";
     MessageType["STREAM_START"] = "stream/start";
